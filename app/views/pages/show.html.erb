<div class="admin-editable">
  <h1 class="page-title"><%= @page.title %></h1>
  <div class="page-body">
    <%= simple_format @page.body %>
  </div>
  <div class="actions">
    <%= edit_link @page %>
    <%= destroy_link @page %>
  </div>
</div>
