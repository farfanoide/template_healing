<h1 class="page-title"><%= @page.title %></h1>
<p>
  <%= simple_format @page.body %>
</p>

<%= link_to t('edit'), edit_page_path(@page) %>
<%= link_to t('destroy'), @page, method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to t('back'), pages_path %>