<%= simple_form_for(@workshop) do |f| %>
  <div class="field">
    <%= f.input :title %><br>
  </div>
  <div class="field">
    <%= f.input :description %><br>
  </div>
  <div class="actions">
    <%= f.submit t('save'), class: :button %>
  </div>
<% end %>
