<!DOCTYPE html>
<html>
<head>

	<title><%= content_for?(:title) ? yield(:title) : "Creation Template" %></title>

  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<%= render 'pages_nav' %>
<div class="wrapper">
    <div class="content">
        <%= render 'flashes' %>
        <%= yield %>
    </div>
</div>
<br />
<%= render 'footer' %>

</body>
</html>
